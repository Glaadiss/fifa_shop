<%= link_to 'Dodaj IP do zablokowanych', new_blocked_ip_path, class: 'btn btn-success' %>
<div class="row">
  <div class="col-md-2"> IP</div>
  <div class="col-md-2"> Nazwa</div>
  <div class="col-md-2"> Opis</div>
</div>
<% @blocked_ips.each do |blocked| %>
    <hr>
    <div class="row">
      <div class="col-md-2"> <%= blocked.ip %></div>
      <div class="col-md-2"> <%= blocked.name %></div>
      <div class="col-md-6"> <%= blocked.descrption %></div>
      <div class="col-md-2">
        <%= link_to 'Usuń', blocked_ip_path(blocked.id), method: :delete, class: 'btn btn-danger'%>
      </div>
    </div>
<% end %>